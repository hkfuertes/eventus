app_token:
  class: sfPropelRouteCollection
  options:
    model:                AppToken
    module:               appTokens
    prefix_path:          /appTokens
    column:               id
    with_wildcard_routes: true

# ShowEvent: POST parameters
# All variables from a user via POST.
#  username -> route & post
#  token -> post
show_event_program:
  url: /:app_token/event/:key/program
  param: { module: events, action: showProgram, app_token: app }

# ShowEvent: POST parameters
# All variables from a user via POST.
#  username -> route & post
#  token -> post
show_event:
  url: /:app_token/event/:key/show
  param: { module: events, action: show, app_token: app }

# CreateUser: POST parameters
# All variables from a user via POST.
create_user:
  url: /:app_token/user/create
  param: { module: users, action: createUser, app_token: app }

# Get User Info: POST and Route parameters
#  username -> route & post
#  token -> post
info_user:
  url: /:app_token/user/info/:username
  param: { module: users, action: getInfo, app_token: app }

# Validate Users: POST and Route parameters
#  username -> route & post
#  password -> post
validate_user:
  url: /:app_token/user/validate/:username
  param: { module: users, action: validateUser, app_token: app }

# default rules
homepage:
  url:   /
  param: { module: default, action: index }
  

